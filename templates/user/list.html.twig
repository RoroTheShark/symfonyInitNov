{% extends "parent.html.twig" %}

{% block contenu %}


    <table>
        <thead>
            <tr>
                <th>Login</th>
                <th>Nom Complet</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{user.username}}</td>
                    <td>{{user.fullname}}</td>
                    <td><a href="{{ path('userEdit', {'id': user.id}) }}">modifier</a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>


{% endblock %}