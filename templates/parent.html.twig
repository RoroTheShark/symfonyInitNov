<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site Critique</title>
    <link rel="stylesheet" href="{{asset("css/style.css")}}">



</head>
<body>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}

    {% if app.user is not null %}
        <p id="welcome">Bienvenue {{app.user.fullname}}</p>
    {% endif %}

    <header>
        <h1>Les critiques de Thomas</h1>
        <cite>{% block citation %}{% endblock %}</cite>
    </header>

    <nav>
        <ul>
            <li><a href="{{ path('articleList')}}">Articles</a></li>
            <li><a href="{{ path('criticsList')}}">Critiques</a></li>
            <li><a href="{{ path('bookList')}}">Livres</a></li>
            <li>A propos de</li>
            <li>
                <a href="{{ path('contact') }}">Contact</a>
            </li>

            {% if is_granted('ROLE_WRITER')%}
                <li><a href="{{ path('bookCreate')}}">Créer Livre</a></li>
                <li><a href="{{ path('articleCreate')}}">Créer Article</a></li>
            {% endif %}
        </ul>
    </nav>

    <main>
        {% block contenu %}{% endblock %}
    </main>

    <footer>
        <p>Site développé durant la formation Symfony</p>
    </footer>
    
</body>
</html>